#include "stdio.h"


int main()
{
    int i, j;
    // char a[20];
    char * a = "world";
    // scanf("%d, %d, %s", &i, &j , a);
    printf("%d, %d, %s \n", i, j , a);

    char * str = "hello";
    printf("%s \n", str);
}
